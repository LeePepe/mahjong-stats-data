# Mahjong Stats Data

Public data store for [LeePepe/mahjong-stats](https://github.com/LeePepe/mahjong-stats).

The web app updates `data/state.json` through the GitHub Contents API. Each accepted edit creates a commit, so the repository history is the durable revision log.
